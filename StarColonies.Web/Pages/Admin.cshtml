@page
@model StarColonies.Web.Pages.AdminModel
@{
    ViewData["Title"] = "Admin - Logs";
}

<div class="container mt-4">
    <h1 class="text-center mb-4">Logs de l'application</h1>

    <div class="table-responsive shadow-sm rounded">
        <table class="table table-striped table-bordered align-middle" id="logsTable">
            <thead class="table-dark">
            <tr>
                <th scope="col" class="text-center">Date/Heure</th>
                <th scope="col" class="text-center">Action (Request)</th>
                <th scope="col" class="text-center">Réponse</th>
            </tr>
            </thead>
            <tbody id="logsContainer">
            @foreach (var log in Model.Logs)
            {
                <tr>
                    <td class="text-center text-nowrap text-white">@log.DateHeureAction.ToString("yyyy-MM-dd HH:mm:ss")</td>
                    <td class="text-break text-white">@log.RequeteAction</td>
                    <td class="text-break text-white">@log.ResponseAction</td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>