@{
    var currentPath = Context.Request.Path.Value?.ToLower();
}
<nav class="navbar navbar-expand-lg navbar-dark mb-4" style="background-color: var(--quaternary-bg-color); border-bottom: 1px solid rgba(255,255,255,0.1);">
    <div class="container">
        <span class="navbar-brand font-barlow fw-bold">PANEL ADMIN</span>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="adminNavbar">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/viewlogs") ? "active" : "")" asp-page="/Admin/ViewLogs">
                        <i class="fas fa-scroll me-1"></i> Logs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/missions") ? "active" : "")" asp-page="/Admin/Missions">
                        <i class="fas fa-list me-1"></i> Toutes les missions
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/addmission") ? "active" : "")" asp-page="/Admin/AddMission">
                        <i class="fas fa-plus-circle me-1"></i> Missions
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/resources") ? "active" : "")" asp-page="/Admin/Resources">
                        <i class="fas fa-list me-1"></i> Toutes les ressources
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/addresource") ? "active" : "")" asp-page="/Admin/AddResource">
                        <i class="fas fa-plus-circle me-1"></i> Ressources
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link @(currentPath.Contains("/admin/users") ? "active" : "")" asp-page="/Admin/Users">
                        <i class="fas fa-users me-1"></i> Utilisateurs
                    </a>
                </li>
            </ul>
            <div class="d-flex">
                <a asp-page="/Dashboard" class="btn btn-sm btn-outline-accent">
                    <i class="fas fa-arrow-left me-1"></i> Retour
                </a>
            </div>
        </div>
    </div>
</nav>


